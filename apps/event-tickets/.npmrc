# Supply-chain defence: never install a version published less than 24h ago.
# A compromised release (the npm worm pattern) is usually pulled within hours;
# this window means our installs skip it instead of being the first to run it.
minimum-release-age=1440
